/**
 * 这三个接口的实现已收到共享包（`@youfeibao/field-shared`）：
 * 收货员现场端与司机端共用同一份自然人准入能力，见 packages/field-shared/README.md。
 *
 * 本文件只做一次再导出，保持既有 import 路径不变（不动调用方）。
 */
export * from '@youfeibao/field-shared/src/api/publicToken'
