-- 每个单元测试结束后清理测试库（BaseDbUnitTest 的 @Sql 会执行本文件）。
--
-- 目前物流模块还没有业务表，所以没有可清理的内容。新增表时**必须**在这里补一条 DELETE，
-- 否则表数据会跨测试残留（H2 的库名按测试上下文唯一，但同一上下文内的多个测试方法共用）。
--
-- 下面的 SELECT 是占位：Spring 不接受只含注释的脚本。首次真实建表时连它一起换掉。
SELECT 1;
