# 反向开票接口对接 · 技术问题清单

**Purpose:** 我们计划接入工商银行融 e 聚的银税协同反向开票接口。13 份接口文档我们已逐份读完，建好了数据模型，但有若干**字段语义在文档之间互相冲突**，还有一批接口文档没拿到。第一条不确认我们无法开始编码——照错的一套建模会让整张数据模型报废。

**From:** 〔你的姓名 / 公司〕　**To:** 工商银行 〔分行〕科技部 / 总行乐企对接人

**How your answers will be used:** 第一条会直接落进我们的数据库表设计；后面几条决定功能做多全。若某条需要转总行产品部门，请告知，我们可以分开跟。

## Context

我们是平台方，不做开票主体，而是提供系统给资源回收企业用，由回收企业以自己名义向自然人出售者反向开票（国家税务总局公告 2024 年第 5 号）。工行侧我们以**合作方**身份接入，回收企业是**子商户**，出售者是**收方**。

已读文档：反向开票技术对接指导手册、收方入驻页面与结果查询、企业授权与发票管理、开票信息预下单、开票信息预查询、开票付方支付、发票下载、发票取消、红字冲销开票、红字冲销确认单撤销、银税协同技术对接指导手册与通知报文说明，另从别处补齐了智慧清分收方新增、智慧清分收方查询、智慧清分收方审核回调三份。

按「预下单 → 自然人确认 → 付款 → 开票 → 下载」这条主链路，我们已把五条状态机（预开票 / 支付 / 缴税 / 上传 / 红字确认单）和各通知类型落进设计。现在缺的是下面这些。

## How to answer

逐条答要点即可。**「不确定」比跳过有用**。如果某条涉及接口行为而需要查代码或问产品，请标注，我们可以等。第一次汇总希望在一周内拿到。

---

## 一、付方编号、收方编号、外部用户编号到底哪个对应哪个

_本组阻塞编码。四份文档指向一套读法，预下单那份指向相反的一套，我们必须先知道以哪份为准。_

### `appIdSub`（付方平台外部编号）是否等同于 `outVendorId`（子商户编号）？

_Why this matters：《聚富通智慧清分收方新增接口》里 `appIdSub` 的备注写的是「同档案参数中 outVenderId 参数」，如果这个等同关系成立，字段映射就唯一确定了。_

>
>

### 预下单接口里 `outUserId` 标注为「付方编号」、`outVendorId` 标注为「收方编号」；但收方入驻、智慧清分收方新增、收方审核回调三份文档里 `outUserId` 指的是自然人用户。以哪一套为准？

>
>

### 在反向开票场景里，`outUserId` 应当上送自然人出售者的编号，还是回收企业的编号？

>
>

### 能否提供《反向开票交互流程图》《正向开票交互流程图》《红字冲销交互流程图》的原始文件？技术对接手册里这几个流程图只有标题，图本身没有随附。

>
>

### `accountKind`（01 对公 / 02 个人）与 `receiverType`（01 企业 / 03 自然人）两个字段是否必须正交上送？再生资源场景下应固定为 `02` + `03` 吗？

>
>

---

## 二、平台经营模式的取值

### `merBusiModel`「01 平台自营」与「02 非平台自营」的判定标准是什么？

_Why this matters：我们平台只提供技术系统，不承担这笔买卖的经营权利义务，开票主体与经营主体都是回收企业。按字面理解应填 `02`，但字段默认值是 `01`，所以不显式上送就会变成「平台自营」，这与我们的实际模式相反。_

>
>

### 我们这种「平台提供系统、回收企业各自以自己名义开票」的模式，应该上送哪个值？

>
>

### 如果上送错误（例如用了默认的 `01`），会有什么后果？会走到不同的业务流程吗？

>
>

### 这个字段与同一次版本新增的 `payChannel` 的 `04 公对公结算` / `05 公对私结算` 是否绑定语义？

>
>

---

## 三、开票档案

### `archiveType` 的「00 平台档案」与「01 子商户档案」在使用上有什么区别？

>
>

### 我们计划一家回收企业开一个子商户档案（`archiveType=01` 且上送 `outVendorId`）。这个模式对吗？

>
>

### `businessType` 的 `0004 再生资源`、`0005 农产品收购`、`0006 开票业务` 三者的差别是什么？`0006` 是不是给「不分子商户」的模式用的？

>
>

### 一个 appid 下最多能挂多少个子商户档案？子商户档案之间能否互相看到数据？

>
>

---

## 四、500 万额度与风控

### 自然人出售者连续 12 个月 500 万元的额度，有没有**事前**校验的接口？还是只能靠开票失败感知？

_Why this matters：税总 5 号公告第三条规定超限不得再开票。如果只能在开票时失败，我们就得自己维护一份滚动额度并承担跨平台不一致的风险；如果有事前查询接口，风控可以前置。_

>
>

### 如果只能靠开票失败感知，失败时的返回码与返回信息是什么？

>
>

### 同一个自然人在多个回收企业、或多个平台上开票，工行侧有统一的额度视图吗？我们能查到吗？

>
>

### 有没有针对自然人出售者的黑名单、涉案或异常名单校验接口？

>
>

### 收方入驻时行内会做哪些校验？我们能提前知道会拒的原因吗？

>
>

---

## 五、发票文件与交付

### 发票下载接口返回哪些格式？PDF、OFD、XML 各支持吗？

>
>

### 支持批量下载吗？还是必须逐笔调用？

>
>

### 下载到的发票如何验签或校验真伪？

>
>

---

## 六、融资能力

### 预下单里的「融资申请编号」、预查询返回的「融资贷款还款总金额 / 还款结果 / 还款时间」、支付方式里的「融资支付」与「新经营快贷」——这些能力是可由合作方主动发起的，还是行内对商户主动授信？

>
>

### 额度与定价由谁确定？风险由谁承担？

>
>

### 平台在这个环节里是什么角色？我们能拿到什么数据？

>
>

---

## 七、缺少的接口文档

以下文档在我们的材料里没有，技术对接手册提到了但未随附，麻烦补发：

- [ ] 融 e 聚正向开票下单接口 V1
- [ ] 融 e 聚开票通用下单接口 V1（手册中标注的路径与上一个接口相同，疑似笔误，请澄清）
- [ ] 融 e 聚退款红冲申请接口 V1
- [ ] 融 e 聚退款即红冲信息查询接口 V1
- [ ] 反向开票货款预支付接口 V1（技术对接手册列为独立接口，但我们手上只有「开票付方支付接口」，两者是同一个吗？）
- [ ] 全部交互流程图原件（反向开票 / 反向开票换开 / 正向开票 / 正向开票换开 / 红字冲销 / 农产品收购 / 灵活用工）
- [ ] 《聚富通钱包业务操作指引》
- [ ] 《合作方 APP 接入钱包开户 H5 页面技术对接手册》
- [ ] 《API 接口对接规范》
- [ ] 《再生资源收方审核异步通知》报文说明

### 另外想确认：付方支付状态查询接口的路径是 `/api/jft/api/payment/queryPaymentStatus/V1` 吗？我们的材料里没有这份文档。

>
>

---

## 八、自然人端的容器与活体模式

_**工行已答复（部分）**：自然人端一期用 H5，后续支持微信小程序，暂不考虑 App；实人认证会跳转到工行的 H5 页面做活体脸部识别；工行 H5 页面可在微信小程序 `web-view` 内打开，自动提交表单也能正常 POST。_

### 实人认证的活体模式，各渠道分别支持哪种？

_Why this matters：我们查到动态活体仅 App 渠道（需集成聚富通 SDK），微信小程序 / 公众号只能用静态活体或无需活体。若反向开票的实人认证必须动态活体，自然人一期就只能上 App，但「不得强制交易对象下载 App」是税务合规口径，两种要求会冲突。_

> **工行答复**：自然人端现在用 H5；实人认证会跳转到工行的 H5 页面进行活体脸部识别认证。后续需支持微信小程序，暂不考虑 App。

### 反向开票场景的实人认证，允许用静态活体或「无需活体」吗？有没有政策或风控上的硬性限制？

> **工行答复**：走工行 H5 页面的活体脸部识别认证（即 H5 可用，不要求集成本地 SDK 动态活体）。

### 实人认证 / 开票确认 / 收方入驻这几个工行 H5 页面，能否在**微信小程序 `web-view`** 内打开？

_Why this matters：小程序的 `web-view` 只能加载已备案、且加入小程序业务域名白名单的域名；需要知道工行页面的域名清单（以及是否支持被嵌入）。不能的话我们只能退回 H5 兜底或改 App。_

> **工行答复**：可以。

### 需要加入微信小程序业务域名白名单的具体域名有哪些？由我方备案还是行方已备案？校验文件如何提供？

> **工行答复**：`yiyoubao.baibaitan.com`，我方备案域名，与工行无关。
>
> 待确认：小程序 `web-view` 里实际加载的是工行页面，是否还需把工行页面域名加入小程序业务域名白名单、校验文件如何交接。

### 这些页面接口返回的是「自动提交表单 HTML」，在小程序 `web-view` 里能否正常 POST 到工行网关？有没有 `X-Frame-Options`、CSP 或 Referer 校验之类的限制？

> **工行答复**：正常 POST。

### `jumpUrl` / `failJumpUrl` 能否定制？跳回地址能否用自定义 scheme / 小程序页面路径，而不只是 https 地址？

> **工行答复**：可以定制跳回地址，但**不支持 scheme**。
>
> 我们的推论：回跳只能是 https 页面。所以 `jumpUrl` / `failJumpUrl` 指向我们自己的 https 落地页（如 `https://yiyoubao.baibaitan.com/seller/#/pages/index/index`），小程序容器在内打开后再由前端路由回具体页面；不能直接用 `weixin://` 或小程序页面路径深链。

### `trxChannel` 的取值与渠道、活体模式如何对应？

_Why this matters：我们按 ADR 0011 暂定 H5 上送 `03`、微信小程序上送 `05`，实人认证结果以通知为准。请确认在各渠道下 `trxChannel` 是否必须与真实容器一致，以及用错会有什么后果。_

> **工行答复**：按渠道上送即可；**用错没有后果**。我们仍按 H5 `03`、微信小程序 `05` 上送。

---

## Anything else?

有没有文档里没写、但对接时一定会踩到的坑？例如容易超时的接口、有调用频率限制的接口、或者必须按特定顺序调用的组合。

>
>
